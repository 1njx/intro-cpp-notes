// _09.14.2025_week3MoreVariables.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
using namespace std;

int main()
{
    int x, y, z;
    x = 0; y = 1; z = 5;
    cout << "the variable x is:" << x << "\n" << "the variable y is:" << y << "\n" << "the variable z is:" << z << "\n" << "x + y + z = " << x+y+z << "\n";
    
    double number1 = 1.5;
    int number2 = 2.1;
    // use the higher priority type when defining the "total"
    double total = number1 + number2;
    cout << total << "\n";
    cout << number1 + number2 << "\n";

    // carat - ^ (exponent)
    // modulus - % (finds the remainder)

}//end
